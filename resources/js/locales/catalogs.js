import { en } from '@/locales/en';
import { ru } from '@/locales/ru';
import { uk } from '@/locales/uk';

export const catalogs = { uk, en, ru };

export const SUPPORTED_LOCALES = ['uk', 'en', 'ru'];
export const DEFAULT_LOCALE = 'uk';
